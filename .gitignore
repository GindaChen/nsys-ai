node_modules/
__pycache__/
*.egg-info/
dist/
# `python -m build` leaves this beside dist/; only dist/ was ignored, so a
# `git add -A` after building a wheel swept the whole staging tree into a commit.
build/
.pytest_cache/
.DS_Store
.venv/
.venv-gpu/

# Generated outputs (use `out/` for viewer/timeline-html/export -o)
out/
trace.json
traces_test/
megatron_viewer_test.html
nvtx_viewer.html
timeline_test.html
timeline_viewer.html
cutracer_out_real/
data/nsys-hero/

*.sqlite
!tests/fixtures/mock.sqlite
!tests/fixtures/healthy_1pct.sqlite
!tests/fixtures/healthy_judged_1pct.sqlite
!tests/fixtures/h100_2gpu_1s.sqlite
!tests/fixtures/mfu_2gpu_before.sqlite
!tests/fixtures/mfu_2gpu_after.sqlite
!tests/fixtures/h100_2gpu_1s.sqlite
*.nsys-rep
*.nsys-cache/
*.nsys-cache.build.lock
*.sqlite.timeline-cache-*.json
examples/*/output/
snapshot_report.html
uv.lock

# Local baseline snapshot store (nsys-ai baseline tag)
.nsys-ai-baselines/

# Agent tool state — contributor-local, not part of the repo.
# .claude/ holds per-machine permission allowlists and live git worktrees
# (~85 MB of checked-out profiles) alongside any local skills; .agent/ is the
# same for other agent tooling. Repo-level guidance lives in AGENTS.md and
# CLAUDE.md. Note .claude-plugin/ is a published artifact and stays tracked.
.claude/
.agent/
